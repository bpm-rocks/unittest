[package]
name=unittest
version=1.0.0
include=libunittest
description=Simplified, lightweight unit testing framework to test Bash functions and shell commands.
license=MIT

[dependencies]
0=assign
1=is
2=strict

[install]
0=unittest
